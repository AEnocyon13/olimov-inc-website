{
  "name": "olimov-inv-hp",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "tokens": "esbuild tools/gen-m3-tokens.mjs --bundle --platform=node --format=cjs --outfile=node_modules/.cache/m3-tokens.cjs --log-level=error && node node_modules/.cache/m3-tokens.cjs"
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@material/material-color-utilities": "^0.4.0"
  }
}
